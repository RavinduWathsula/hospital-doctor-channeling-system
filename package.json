{
  "name": "smart-hospital",
  "version": "1.0.0",
  "description": "Smart Hospital Doctor Channeling & Appointment Management System",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "node start.js",
    "start": "node start.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
